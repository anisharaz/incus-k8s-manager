# incus-k8s-manager

A kubernetes(k8s) manager based on `incus`.

Goals of this project.

1. create k8s cluster with incus.
2. manager certain incus setting relevant for k8s cluster using UI.
3. Make this a simple, single docker container, easy to use and deploy. The docker container will include everything i.e. incus, and this project.
